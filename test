#!/usr/bin/env bash
# ──────────────────────────────────────────────
#  Crypto Strategy Backtester — Web UI Launcher
#  Just double-click this file (or run: ./test)
# ──────────────────────────────────────────────

DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$DIR"

# Activate venv
source .venv/bin/activate

echo ""
echo "  🔮  Starting Crypto Strategy Backtester..."
echo ""

# Launch the web server (auto-opens browser)
python web_server.py "$@"
